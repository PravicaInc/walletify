import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  tabsHeaderContainer: {
    width: '100%',
    height: 32,
    justifyContent: 'center',
    shadowOpacity: 0,
  },
  tabHeaderItem: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});

import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    width: '100%',
    height: 6,
    flexDirection: 'row',
  },
  progressItem: {
    flex: 1,
    borderRadius: 13,
    marginHorizontal: 2,
  },
});

export default styles;

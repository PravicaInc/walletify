import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  headerContainer: {
    paddingTop: 22,
    paddingHorizontal: 20,
  },
  accountsList: {
    paddingVertical: 10,
    paddingHorizontal: 20,
  },
});
